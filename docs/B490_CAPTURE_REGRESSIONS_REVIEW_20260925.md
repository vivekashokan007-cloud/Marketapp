# b489 capture regressions — Codex review patch, 25 September 2026

Base: Marketapp `cc5fa9cace39d35c9cf01133c57eb9e358e2bf96`; MarketVivi `c56bf1869e1051e158703969f2b00c48f5f4199f`.
Scope: isolated review branch `work/b490-capture-regressions-codex-20260925`.
Status: prepared locally; no production DDL, push to main, release or Pages deployment.

## Verified baseline

- Read-only production `information_schema` returned 24 `position_ticks` columns, omitting `quantity_units`, `contract_lot_size`, `number_of_lots`, `lot_authoritative`. The row producer at `cc5fa9c` sends all four. Read-only `max(tick_ts)` remained `2026-09-24 06:31:55.581+00`; no ticks after 06:32 UTC. The schema mismatch is sufficient to explain the PostgREST 400. The user's phone queue size and unrecovered gap require device readback.
- Read-only `ml_brain_snapshots` from 24 September onward: 0/92 included `snapshot_position_verdicts`, 0/92 included `snapshot_capture_completeness`. Python includes both, Kotlin dropped them in `compactBrainSnapshot`.
- The Paper P1 bridge omitted gross `peak_pnl` and `trough_pnl`, and Android wrote null for missing optional fields. `PositionTickService.computePositionTickCurrentPnl` uses entry and executable mark multiplied by quantity with no friction deduction: its stored P1 P&L is gross MTM, matching `trades_v2` extrema.

## Patch

1. Additive `supabase/migrations/20260925100000_position_ticks_lot_columns.sql` with numeric lot fields, boolean authority and `notify pgrst, 'reload schema'`. It has **not been applied**. `docs/contracts/position_ticks_columns.json` records the planned columns; re-export it from production after migration. The tick producer schema test reads literal top-level keys directly from `buildTickRow` and checks the contract.
2. One `BATCH_A_CONTEXT_KEYS` list in `EvaluationLocalCache` keeps all 18 Batch A survivors from Python when present. The test fixture was generated by the real `brain.take_poll_snapshot(..., 'android_compact_v1')` call with one synthetic open Paper position. A JVM parity test reads the Python survivor declarations and checks the Kotlin list covers them.
3. For validated Paper P1 only, compute gross peak, trough and erosion with the existing chain-path rules, and record corrected VIX change and provenance in `position_live`. Restore gross peak/trough to the in-memory trade. Android updates optional stored metrics only for present finite values. The existing Batch A policy continues to keep `vixChange` and `peakErosion` **observation only**; feeding them into `position_verdict` would be a separate advice change. Real trade valuation path is unchanged.

## Red / green evidence and limitations

- New Python three-poll test at baseline: **FAIL**, `KeyError: 'pnl_basis'` on first P1 mark. With patch: 16 P1 tests **OK**; full Python suite: **1,054 OK, 2 skipped**.
- Static schema comparison on baseline: four emitted tick keys absent from 25 September live schema. Against planned migration contract: 26/26 emitted keys covered. This does not replace a live post-migration insert/readback.
- Existing MarketVivi `tests/test_trades_v2_insert_schema_contract.mjs`: **PASS** (77 literal keys, 18 stripped, 0 unknown) against the existing `trades_v2` column list. Android does not insert into `trades_v2`; this PWA guard already covers that path.
- Android JVM tests: **BLOCKED** on this runner. `./gradlew :app:testDebugUnitTest --offline --no-daemon` attempts to download uncached Gradle 8.7; network to `services.gradle.org` is unreachable. The new JVM tests must run in CI or a configured Android build box before a b490 release. `git diff --check` passed.
- The Python tests that previously rewrote `docs/batch_c_policy_eval/sample_policy_eval_report_20260923.json` and created `tests/fixtures/batch_b_parity/parity_observations.jsonl` now use temporary paths. A fresh full-suite run left both paths clean.
- The most recent persisted snapshot's `snapshot_android_compaction` records `advice_parity_observed`, `position_state_observed`, `snapshot_open_trades_json` and additional arrays as `dropped_over_budget`, with final context size 790,433 bytes against a 1,350,000 byte Python cap. The removed sections' original byte sizes cannot be recovered from these compacted rows; report them from raw pre-compaction evidence before deciding whether to slim them.

## Production migration decision and verification

Production DDL was attempted on 26 September after Vivek said “Continue”, but automatic approval review rejected it: earlier retained user instructions explicitly ban Supabase writes/migrations, and “Continue” did not clearly override that specific ban. No migration occurred and no workaround was attempted. Obtain explicit approval for the four named columns before applying the migration and before any b490 release. Then read `information_schema.columns` for exact names/types, observe new HTTP 201 responses to `/position_ticks`, and query read-only ticks after `2026-09-24 06:32:00+00`. A schema change alone cannot prove the phone's queued rows drained. Report any rows lost after the 1,500-row queue cap was reached. The snapshot capture fix requires a new Android build and an open position to verify its persisted fields; no historical rows are relabeled.

Version remains 2.6.58 / b489 while the Android test gate and production migration are pending. No b490 release claim is made.
